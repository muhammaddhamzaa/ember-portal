{{#each-in metaData as |module definition|}}
  {{#if (eq definition.name "cancel_button") }}
  <button class="btn btn-inverse" {{action "changeToAccounts"}}> Accounts </button>
  {{/if}}
{{/each-in}}
{{yield}}
