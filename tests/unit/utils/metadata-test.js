import metadata from 'ember-portal/utils/metadata';
import { module, test } from 'qunit';

module('Unit | Utility | metadata', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = metadata();
    assert.ok(result);
  });
});
